apiVersion: apps/v1
kind: Deployment
metadata:
  name: fixtures
  namespace: fpl-agents
spec:
  replicas: 1
  selector:
    matchLabels: { app: fixtures }
  template:
    metadata:
      labels: { app: fixtures }
    spec:
      containers:
        - name: fixtures
          image: fpl-agents/fixtures:local
          imagePullPolicy: Never
          ports: [{ containerPort: 8000 }]
          envFrom:
            - configMapRef: { name: fpl-config }
            - secretRef: { name: fpl-secrets }
          env:
            - name: FIXTURES_AGENT_DISABLE_LLM
              value: "1"  # flip to "0" once real Foundry credentials are in secret.yaml
          readinessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: fixtures
  namespace: fpl-agents
spec:
  selector: { app: fixtures }
  ports: [{ port: 8000, targetPort: 8000 }]
