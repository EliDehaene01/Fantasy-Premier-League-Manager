apiVersion: apps/v1
kind: Deployment
metadata:
  name: template
  namespace: fpl-agents
spec:
  replicas: 1
  selector:
    matchLabels: { app: template }
  template:
    metadata:
      labels: { app: template }
    spec:
      containers:
        - name: template
          image: fpl-agents/template:local
          imagePullPolicy: Never
          ports: [{ containerPort: 8000 }]
          envFrom:
            - configMapRef: { name: fpl-config }
            - secretRef: { name: fpl-secrets }
          env:
            - name: TEMPLATE_AGENT_DISABLE_LLM
              value: "1"  # flip to "0" once real Foundry credentials are in secret.yaml
          readinessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: template
  namespace: fpl-agents
spec:
  selector: { app: template }
  ports: [{ port: 8000, targetPort: 8000 }]
